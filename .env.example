# URL pública (wss://) do Worker Cloudflare do Multiplayer (Sprint MP-04).
# Sem isso, o cliente cai no fallback de desenvolvimento local (servidor Bun).
VITE_MULTIPLAYER_WS_URL=wss://conflito-multiplayer.<seu-subdominio>.workers.dev/connect
